// export function isKeyDuplicated(error: unknown) {
//  if (error.code === 11000) {
//     if (error.keyPattern?.name) {
//       throw new Error(`Команда с названием "${team.name}" уже существует`);
//     }
//     if (error.keyPattern?.urlSlug) {
//       throw new Error(
//         `Slug для команды "${urlSlug}" уже занят. Попробуйте изменить название`
//       );
//     }
//     throw new Error(`Конфликт уникальности: ${JSON.stringify(err.keyValue)}`);
//   }
// }
