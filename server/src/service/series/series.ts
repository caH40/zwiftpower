/**
 * Сервис получения серий заездов.
 */
export async function getNSeriesService() {}

/**
 * Сервис получения серии заездов.
 */
export async function getNSeriesCurrentService({ urlSlug }: { urlSlug: string }) {
  console.log('urlSlug', urlSlug); //eslint-disable-line
}
