# Алгоритм расчета и установки zpruPoints - рейтинговых очков сайта

### 1. ⭐ Определение важности События (event) и коэффициент.

Устанавливается свойством `importanceLevel` в сущности `ZwiftEvent`, принимает следующие
значения:

```typescript
type ImportanceCoefficientsLevels = 'unrated' | 'standard' | 'important' | 'championship';
```

| Название     | Уровень            | Пояснение                |
| :----------- | :----------------- | :----------------------- |
| unrated      | **Не рейтинговый** | Коферайды, тренировки    |
| standard     | **Базовый**        | Стандартные соревнования |
| important    | **Приоритетный**   | Важные старты            |
| championship | **Премиальный**    | Финалы, чемпионаты       |

### 2. 🎯 Коэффициент массовости заезда/группы (K)

Рассчитывается суммой финишировавших участников в соответствующей категории, если заезд делится
на категории, или в самом заезде, если общий протокол.

`massCoefficient` рассчитывается каждый раз перед расчетом и установкой zpruPoints в результаты
События.

| Участников  | Коэффициент (K) | Статус заезда  |
| :---------- | :-------------: | :------------- |
| **1 — 3**   |     **0.0**     | Не рейтинговый |
| **4 — 14**  |     **0.5**     | Малый          |
| **15 — 39** |     **1.0**     | Стандартный    |
| **40 — 99** |     **1.8**     | Крупный        |
| **100+**    |     **2.5**     | Гигантский     |

### 3. 🏭 Выбор сформированных результатов События.

Существуют два типа результатов:

- **стандартный**: формируется для любого События. Может модерироваться установкой
  дисквалификации участникам заезда;
- **результат этапа**: отдельная сущность, которая формируется для этапов серий заездов для
  которых реализован соответствующий функционал.

**Результат этапа** определяется флагом `useStageResults` в сущности `NSeries`. То есть не все
серии имеют отдельные результаты этапов. События вне серий имеют только **стандартный**
результат.

### 4. Перерасчет рейтинга команд

Запускается каждый раз при формировании результатов рейтинговых Событий. Хранится в БД в
сущности `TeamSeasonRating`

```typescript
type TeamSeasonRating = {
  _id: Types.ObjectId;
  team: Types.ObjectId; // Ссылка на команду.
  season: string; // 2024-2025, 2025-2026 и.т.д.
  rank: number; // Место команды в таблице рейтинга.
  points: number; // Командные очки за сезон.
  eventsIds: Types.ObjectId[]; // _id Эвентов в БД;
  createdAt: Date; // Дата создания записи.
  updatedAt: Date; // Дата обновления записи.
};
```
